ALLOW_IP_RANGE=0.0.0.0/0
POSTGRES_USER=skanda-user
POSTGRES_PASS=skanda
POSTGRES_DBNAME=housing_survey

#sets the password env variable so createdb commands succeed in dockerfile w/out pass
PGPASSWORD=skanda
